pydantic>=2.12.5
google-genai>=1.67.0
python-dotenv>=1.2.2

# graph_enterprise (Microsoft Graph)
msal>=1.35.1
httpx>=0.28.1
tzdata>=2026.1

# HTML body extraction (optional fallback to stdlib HTMLParser if unavailable)
beautifulsoup4>=4.12.3
lxml>=5.3.0
# Next.js admin UI (web/) uses FastAPI below
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
passlib[bcrypt]>=1.7.4

# Microsoft SQL Server (required for config UI, API, scheduler, run logs)
pyodbc>=5.1.0

# Celery scheduler + workers (Redis broker)
celery[redis]>=5.4.0
redis>=5.0.0