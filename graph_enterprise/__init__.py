"""
Enterprise Microsoft Graph pipeline for inbox classification (scaffold).

Implements the architecture in PROJECT_DOCUMENTATION.md Part 3: app-only Graph
access, per-mailbox config, fetch → preprocess → classify → write-back,
structured run logs. The local Outlook prototype remains
in inbox_classifier.py at the repo root.
"""

__all__ = ["__version__"]

__version__ = "0.1.0"
